version: 2.1
jobs:
  build:
    docker:
      - image: node:14.20.2
      - image: cimg/base:2022.06
        auth:
          username: $DOCKERHUB_USERNAME
          password: $DOCKERHUB_PASSWORD
    steps:
       - checkout
       - run:
       - run:
           command: npm run test
           name: Run tests
    # ... steps for building/testing app ...
       - setup_remote_docker:
           docker_layer_caching: true
           version: 20.10.14
       - run:
           name: Build docker
           command: npm ci:docker

workflows:
  version: 2
  build:
    jobs:
      - build